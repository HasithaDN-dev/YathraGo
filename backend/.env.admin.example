# Admin Environment Variables

# Add these to your backend/.env file

# ============================================
# ADMIN JWT CONFIGURATION
# ============================================

# Admin JWT Secret - Use a strong random string
# If not set, will fallback to WEB_JWT_SECRET
ADMIN_JWT_SECRET=your-super-secret-admin-jwt-key-change-this-in-production-use-random-64-chars

# Admin JWT Expiration (default: 8h)
ADMIN_JWT_EXPIRES_IN=8h

# ============================================
# BACKUP CONFIGURATION (for upcoming backup module)
# ============================================

# Directory where backups will be stored
BACKUP_DIR=./backups

# Backup schedule (cron format) - Daily at 2 AM
BACKUP_SCHEDULE=0 2 * * *

# ============================================
# ADMIN SESSION CONFIGURATION
# ============================================

# Maximum number of concurrent sessions per admin
MAX_ADMIN_SESSIONS=5

# Session timeout (hours)
ADMIN_SESSION_TIMEOUT=8

# ============================================
# NOTES
# ============================================

# 1. Generate a secure ADMIN_JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# 2. Keep ADMIN_JWT_SECRET different from WEB_JWT_SECRET for security
#
# 3. Never commit .env file to git
#
# 4. Update these values in production environment
